# -*- coding: utf-8 -*-
u"""_zf121_docs.py —— ZF121 的文档三件套（锚点替换，逐处断言"原文只出现一次"）

改四处：
  ① `docs\\开发档案.md` §4 新增 **4.97**（把"活体数字"当配方参数用 ⇒ 改一个人家的数）；
  ② 同文件 §5 表格追加 **ZF121** 行；
  ③ 同文件 §9 追加 **ZF121** 小节（插在 `## 10. 备份策略` 之前）；
  ④ `docs\\多会话协作交接.md`：§1 活体数字 / §2 最近动作 / §3 快照口径 / §5.2 盔甲线提醒 / §6 欠账。

跑法：
    python build\\zftools\\_zf121_docs.py            # 只校验
    python build\\zftools\\_zf121_docs.py --write    # 落盘
"""
import io
import os
import sys

try:
    sys.stdout.reconfigure(encoding="utf-8", errors="replace")
except Exception:
    pass

ROOT = r"E:\PotatoST"
DOC = os.path.join(ROOT, r"docs\开发档案.md")
HAND = os.path.join(ROOT, r"docs\多会话协作交接.md")

# ============================================================
#  ① §4 新增 4.100（**插在 `## 7.` 之前** —— 4.96 已被并行那条线占了）
#     ⚠ 本轮开工时并行线（ZF120 振金套）往这里插了自己的 4.96 ⇒ 我改号到 **4.97**，
#       锚点只认 `## 7.` 这个稳定的节标题（他们再改自己的段落也打不到我）。
# ============================================================
A4_OLD = u'''## 7. 权威情报来源（怎么查原版行为，别靠记忆）'''

A4_NEW = u'''### 4.100 【连带雷】把"活体数字"当配方参数用 ⇒ 改一个人家的数（0.11 ZF121，探针当场抓到）

`AlloySmelterRecipes.MAX_ENERGY_PER_TICK` 是**全表最贵那条**的每 tick 耗电（给静态守卫读的），
本来就是个"会随新配方长大"的**活体数字**。可 ZF111 写星璨钢那条时，最后两个参数写的是：

```java
DURATION_TICKS, MAX_ENERGY_PER_TICK));   // 写的时候它正好等于 12000
```

于是 ZF121 的振金那条把它抬到 14500 之后，**星璨钢那条也一起变成了 14500** ——
用户 ZF111 白纸黑字给的是 12000。**三处静态检查（含我这一轮新写的"MAX 必须真的是全表最大"）
全都是绿的**：两条配方引用的是同一个符号，从符号层面看怎么都对。

抓住它的是**真游戏探针**：跑一轮星璨钢、按 12000 FE/t 喂电 ⇒ 实扣 **7,206,500**（一轮都没跑完）、
输出也不是 3 个；再顺手把"第①②③条仍是 800/800/12000"那条断言顶红了。

**规矩**：

1. **用户给的数就写死成它自己的常量**（`STAR_STEEL_ENERGY_PER_TICK = 12_000`、
   `VIBRANIUM_ENERGY_PER_TICK = 14_500`）；`MAX_ENERGY_PER_TICK` 只给 static 守卫读，
   **配方表不许引用它** —— 常驻检查直接钉这一条，反证刀 K172/K173 分别咬两处。
2. **派生量（最大值 / 总数 / 键数）永远不要被具体条目引用**：派生量会动，条目不该动。
   同理，凡是"某条具体配方/某个具体物品的数"，都别借"全表最大/合计"那个符号。
3. **"符号引用正确"不等于"数值没变"**：探针的期望值一定要写**用户说的字面量**（§4.27）——
   这次就是它救的场（三条静态检查全绿、真游戏一跑就红）。

## 7. 权威情报来源（怎么查原版行为，别靠记忆）'''

# ============================================================
#  ② §5 表格追加 ZF121 行（插在 ZF119 行之后）
# ============================================================
A5_ANCHOR = u'''| 见 §9 |
'''

A5_ANCHOR_TAIL = u'''| 见 §9 |



> ZF40~ZF44 全是**电力高炉的连续改动**'''

ROW = u'''| ZF121 | **新建 `zf121_pre`**（**215 份**改前件：`PotatoST` / `AlloySmelter{Recipes,BlockEntity,Menu}` / `MachineRecipes` / `client\\AlloySmelterScreen` / `client\\jei\\*` + 生成器 `GenCommonTags.py` + **整棵 `data\\c\\tags` 树** + 四份 lang + **全部常驻校验脚本** + 3 份文档 + 旧成品 jar 与 `.sha1`；逐份核哈希 + **回读证明**（215 份与盘上逐字节相同）、失败 0） | 0.11：**振金的合金冶炼炉配方**。用户原话（第一版）：「振金合金冶炼炉配方；1硬质钛合金+8热力金属+2高碳钢+3银锭+12金锭 粗振金+钻石+2下界合金碎片+1红石粉 14500Fe/t 产出1振金」；**紧接着自己改口**：「对不起刚才忘了合金炉的限制 …粗振金+2下界合金碎片14500Fe/t 产出1振金」。① **消耗品从 4 样收窄成 2 样**（粗振金 ×1 + 下界合金碎片 ×2）⇒ **槽位数一个都没动**（仍 5 输入 / 3 输出 / 2 消耗槽）—— 我按第一版已经把消耗槽扩到 4 个、并撤了菜单那层门，用户改口后**把槽位改动整个撤掉**（两代改动留档：`_zf121_java_v1.py` + **反推证明 6/6**：当前盘 = 改前件 + v1 + v2 + 最终版，四代同堂逐字节回得去）；② **两处地基先补**：`硬质钛合金`（ZF104）与 `热力金属`（ZF45）**本来都不在 `#c:ingots` 里** —— 而输入槽只收这个标签 ⇒ 这两样**放都放不进去**、配方永远开不了工，且静态检查看不出来（表里只是个 TagKey，与 ZF111 踩的 `c:ingots/netherite` 同一类坑）⇒ 登记进 `GenCommonTags.py` 的 `ALLOYS` 表；顺带**补掉两处「表 ↔ 盘」漂移（§4.93 第 3 次）**：ZF114 手写的 `c:raw_materials/vibranium`、ZF119 手写的 `c:ingots/vibranium` 都没登记进表 ⇒ 生成器一跑父标签里那两行就被静默抹掉（第一跑实测：`c:ingots` 少了 vibranium_ingot、`c:raw_materials` 少了 raw_vibranium）⇒ 振金登记进 `METALS` 表（锭 + 粗矿、没有矿石方块），反向体检从此 **item/block 类手写标签 = 0 份**；③ **菜单那层门撤掉**：ZF49 的「目前放不了东西」在 ZF111 放开方块实体之后**还留在菜单里**（`mayPlace` 恒 false）⇒ 手动一个都放不进去（只有管道/漏斗塞得进），而 ZF111 与本轮两条配方都要求消耗槽里有东西 —— 那是条死路；现在交给 `machineInventory.isItemValid` 判（没激活照样 false、垃圾照旧进不去），并让消耗品也能 shift 点击；④ **探针当场抓到一个真雷（§4.100）**：星璨钢那条原来借 `MAX_ENERGY_PER_TICK` 当自己的每 tick 耗电 ⇒ 本轮把它抬到 14500 时**星璨钢也偷偷从 12000 变成 14500**（用户 ZF111 给的是 12000），三处静态检查**全绿**、只有真游戏跑出来才现形 ⇒ 两条数各拆各的常量（`STAR_STEEL_ENERGY_PER_TICK` / `VIBRANIUM_ENERGY_PER_TICK`），**配方表不许引用 MAX**；⑤ **活体数字**：**语言键一个都没加、没删**（仍 **454 键** —— 那个数并行那条线刚从 449 加上去 5）⇒ **零份键数耦合的校验需要 retarget**；配方文件数也不动（合金炉是 Java 表，不是数据包配方）⇒ 9 份配方名单门一份都不用碰；⑥ 证据：真服务端探针 `Zf121Check`（**73 项 ALL OK**）、`_zf121_verify.py`（**130 项**）、反证 **12 把刀**（K166~K177，逐把咬中指定检查）| 见 §9 |'''

# ============================================================
#  ③ §9 追加 ZF121 小节（插在 `## 10. 备份策略` 之前）
# ============================================================
A9_ANCHOR = u'''## 10. 备份策略'''

A9_NEW = u'''### ZF121（0.11）振金的合金冶炼炉配方（+ 补两处标签地基 + 拆一个能量常量的雷）—— **未打包**

原话（第一版）：「振金合金冶炼炉配方；1硬质钛合金+8热力金属+2高碳钢+3银锭+12金锭
粗振金+钻石+2下界合金碎片+1红石粉 14500Fe/t 产出1振金」

原话（**改口·最终**）：「对不起刚才忘了合金炉的限制 这是新振金合金冶炼炉配方；
1硬质钛合金+8热力金属+2高碳钢+3银锭+12金锭 粗振金+2下界合金碎片14500Fe/t 产出1振金」

| 输入（5 个输入槽，只收锭） | 消耗（2 个消耗槽） | 产物 | 耗电 |
|---|---|---|---|
| 硬质钛合金 ×1 ＋ 热力金属 **×8** ＋ 高碳钢 ×2 ＋ 银锭 ×3 ＋ 金锭 **×12** | 粗振金 ×1 ＋ 下界合金碎片 ×2 | **振金锭 ×1** | **14500 FE/t** |

**⚠ 时长你没给** ⇒ 沿用本机规格 **30 秒（600 tick）** ⇒ 一件总耗电 **14500 × 600 = 8,700,000 FE**。
这个数是**我按本机规格补的**（要改是一个参数，说一声）。顺带把账算给你看：机器储能 32768
⇒ 满缓冲只够 **2.26 秒**，也就是说振金必须**持续供上 14500 FE/t**（低级发电机 100 FE/t ⇒ 得 145 台；
合金炉自己的接线口能吃满就行）。金锭走 NeoForge 自带的 `c:ingots/gold`（解包核过），
下界合金碎片按**具体物品**认（`Items.NETHERITE_SCRAP`，与 ZF111 的末影水晶同一条口径）。

#### 一、你改口那一下，我把槽位改动**整个撤掉了**

你第一版给了 **4 样**消耗品（粗振金 + 钻石 + 2 下界合金碎片 + 1 红石粉），而本机只有 **2 个消耗槽**。
我当时按"实现你的字面要求"把消耗槽从 2 个扩到 4 个（界面摆成 2×2、老槽位序号不动、老存档不丢东西），
刚改完你就补了一句「对不起刚才忘了合金炉的限制」并把配方收窄成 **2 样**。

⇒ **槽位数一个都没动**（仍是 ZF49 定的 5 输入 / 3 输出 / 2 消耗槽），2 样消耗品正好把 2 个槽用满。
两代改动都留了档（`_zf121_java_v1.py` = 扩槽那一版），并且把这套"改到一半被用户改口"的过程
做成了**可验证的反推证明**：当前盘上的文件逐字节等于「改前件 + v1 + v2 + 最终版」四代叠加
（`_zf121_java.py --proof`，6/6 通过）—— 这样"撤掉"不是"删了重写"，而是有账可查。

#### 二、两处**地基**先补上（不补的话这条配方根本放不进去）

| 缺什么 | 为什么是致命的 | 怎么补的 |
|---|---|---|
| `硬质钛合金`（ZF104）**不在 `#c:ingots` 里** | 输入槽（ZF49 用户原话「只能接受锭标签」）只收 `#c:ingots` ⇒ 这格**放不进东西**、配方永远开不了工；静态检查看不出来（表里只是个 TagKey） | 登记进 `GenCommonTags.py` 的 `ALLOYS` 表 ⇒ 生成 `c:ingots/hard_titanium_alloy` + 扁平写法，并进父标签 |
| `热力金属`（ZF45）**不在 `#c:ingots` 里** | 同上（它当年只有"加热装置"一个下游，从没进过锭标签） | 同上 |

⚠ **硬质钛合金挂的是 `c:ingots/hard_titanium_alloy`，故意没有并进 `c:ingots/titanium_alloy`** ——
那一格是配方②（轻质钛合金 + 高碳钢 + 镍锭 → 硬质钛合金）的输入，并进去就不只是"兼容别的 mod"了：
它会变成「硬质钛合金 → 硬质钛合金」的**复制漏洞**。探针专门有一条③d 在验这个（反证 K… 也盯着）。

**顺带补掉两处「表 ↔ 盘」漂移（§4.93 第 3 次）**：第一次跑生成器时实测
`c:ingots` **少了** `vibranium_ingot`（ZF119 手写的）、`c:raw_materials` **少了** `raw_vibranium`
（ZF114 手写的）—— 两轮都是**直接改父标签 JSON、没登记进 `ALLOYS`/`METALS` 表**。
本轮把振金登记进 `METALS`（锭 + 粗矿，**没有矿石方块** —— 粗振金是陨石砸出来的），
那三份手写文件从此由生成器拥有：以后谁重跑都不会漂，反向体检也从"3 份多余文件"变成
**item/block 类手写标签 = 0 份**。同时给生成器补了 `sys.stdout.reconfigure`（它原来一 print 那句
`⚠ 这些 c: 标签文件不是本脚本写的` 就 GBK 崩栈 —— 文件写完了、**结论永远看不到**，本轮就踩了）。

#### 三、菜单那层门：ZF111 放开了一半

ZF49 立的规矩是「2 个消耗槽（目前放不了东西）」，那道门其实有**两层**：

| 层 | ZF49 | ZF111 | ZF121（本轮） |
|---|---|---|---|
| 方块实体 `isItemValid` | 恒 false | 改成"某条配方真的会消耗它"才收 | 不变 |
| 菜单 `SlotItemHandler.mayPlace` | 恒 false | **没动**（还是恒 false） | **撤掉**，交给 `isItemValid` 判 |

⇒ 结果是：ZF111 那条星璨钢配方**要求消耗槽里有东西**，可玩家**手动一个都放不进去**
（只有管道/漏斗塞得进）—— 那是条死路，我在 ZF111 那轮的"要你实测的"里还写着"2 个消耗槽放深层钴矿石 +
末影水晶"，其实是做不到的（**如实记下来**）。本轮把那层撤掉：能不能放由 `isItemValid` 说了算
（没激活照样 false、垃圾照旧进不去，"不能当第二个背包"这条口径没变），并让消耗品也能 shift 点击。

#### 四、⚠ 探针抓到的真雷：`MAX_ENERGY_PER_TICK` 被当配方参数用了（新雷 §4.100）

星璨钢那条的每 tick 耗电写的是 `MAX_ENERGY_PER_TICK`（ZF111 写它时那个常量正好 12000）。
本轮振金那条要 14500 ⇒ 常量抬到 14500 ⇒ **星璨钢也一起变成了 14500**。

**三处静态检查全绿**（连我新写的"MAX 必须真的是全表最大"都是绿的 —— 两条引用的是同一个符号），
是**真游戏探针**把它顶红的：按 12000 FE/t 喂电跑一轮星璨钢 ⇒ 实扣 **7,206,500**、一轮跑不完、
输出不是 3 个。修法：两条数各拆各的常量（`STAR_STEEL_ENERGY_PER_TICK = 12_000` /
`VIBRANIUM_ENERGY_PER_TICK = 14_500`），`MAX_ENERGY_PER_TICK` 只给 static 守卫读，
**配方表不许引用它**（常驻检查 + K172/K173 两把刀）。

#### 五、证据

| 项 | 值 |
|---|---|
| 探针 | `Zf121Check.java`（真 `runServer`）**73 项 ALL OK**：五个输入标签非空且认得人（含原版金锭）/ 两个父标签（顺带记下运行时件数：`c:ingots` 16 件、`c:raw_materials` 13 件 —— 我们各占 12 / 10）/ 配方表逐项（8 热力金属、12 金锭、2 消耗品、**钻石与红石粉不在里面**、1 振金锭、14500 × 600 = 8,700,000）/ **消耗槽门禁**（收粗振金与碎片、不收圆石/**不收钻石与红石粉**）/ **缺消耗品 ⇒ 不开工且一度电都不扣** / 只放一半也不开工 / 齐了跑满 600 tick **正好扣 8,700,000 FE**、五样输入与两样消耗按数目扣干净、输出正好 1 个振金锭 / **防复制**（硬质钛合金+高碳钢+镍锭凑不出配方）/ 老配方仍 800、**星璨钢那条仍 12000** |
| 探针存档 | `build\\zftools\\check\\Zf121Check.java`（**先抄后删**） |
| 常驻校验 | `_zf121_verify.py`（**130 项**）：改前件账目 / 7 份标签内容 / 两个父标签项数与内容 / 配方表逐项 / **四条配方各自的每 tick 耗电 = 800·800·12000·14500** / **没有任何配方引用 MAX** / 槽位数没动 / 菜单那层门撤掉 / 四语言（键集合一致、**备份里的键一个没少**、相对备份只动了脚注那一个值、摆放图逐字未动）/ 探针报告 / 文档 / 往轮门 retarget |
| 反证刀 | **K166~K177（12 把）**：热力金属 8→7 / 碎片 2→1 / 把钻石加回消耗品 / 删硬质钛合金标签文件 / 父标签少热力金属 / **复现 ZF114 的漂移**（父标签少粗振金）/ 第④条回去借 MAX / 星璨钢回去借 MAX / **复现 ZF111 的老问题**（菜单重新拦死）/ **复现我第一版的扩槽**（消耗槽偷偷扩回 4）/ 脚注 14500→12000 / 删那条 tooltip 键 —— 每把都必须咬住**指定的那一条**检查 |
| 活体数字 | 语言键 **仍 454**（一个没加没删 ⇒ 零份键数耦合校验要改）；配方文件数不动；`GenCommonTags.py` 表 +2 条合金 +1 条金属 |
| ⚠ 并行那条线 | 本轮开工时**同一棵树上还有一条线在做 ZF120（振金套）**：`_zf120_*.py` 已在盘上、lang 已经从 449 变 454、`ModVibraniumSet.java` 一度处在编译不过的中间态（我的第一次 `runServer` 撞上他们的编译窗口，报 `Failed to load class com.potatost.mod.PotatoST` —— 那是**并发编译竞态**，不是本轮的代码问题，重跑即绿）。本轮**没有碰** `_zf120_*` 任何文件，也没有采纳他们的轮号（我用 ZF121）|

**要你实测的**（进游戏）：摆一台成型合金炉 ——

1. **5 个输入槽**：硬质钛合金 ×1、热力金属 ×8、高碳钢 ×2、银锭 ×3、金锭 ×12；
   （硬质钛合金与热力金属这轮才挂上 `#c:ingots`，**以前是放不进输入槽的**，现在应当放得进去）
2. **2 个消耗槽**：粗振金 ×1、下界合金碎片 ×2 —— ⚠ 这两格**这轮才第一次能用手放**
   （ZF111 那会儿只有管道塞得进）；
3. 接够电（≥14500 FE/t，缓冲只有 32768 ⇒ 得持续供）⇒ 30 秒后输出槽里 **1 个振金锭**；
4. 顺手看一眼**老配方没被弄坏**：铝+钛+银 → 轻质钛合金仍是 800 FE/t；
   星璨钢那条仍是 **12000 FE/t**（不是 14500）。
5. 缺消耗品时它**不会开工**、也不扣电 —— 这是设计，不是 bug。

**成品**：**本轮没有新成品** —— `release\\PotatoST-0.11.jar` 仍是 ZF114 打的
**`303c5d468b96826ef6836b0a4e54ccb8a539557c`**（432 键，**不含 ZF116~ZF121**）。

## 10. 备份策略'''

# ============================================================
#  ④ 交接文档
# ============================================================
H1_OLD = u'''| 语言键数 | **449 键 × 4**（zh_cn / en_us / ja_jp / ru_ru，四份键集合必须完全一致） | 翻译润色线正在重写**值**；键数的活体数字：… → 432（ZF114）→ 448（ZF117）→ **449**（ZF119 振金锭） |'''
H1_NEW = u'''| 语言键数 | **454 键 × 4**（zh_cn / en_us / ja_jp / ru_ru，四份键集合必须完全一致） | 键数的活体数字：… → 448（ZF117）→ 449（ZF119 振金锭）→ **454**（ZF120 振金套 +5）。⚠ **ZF121 一个键都没加没删**（改的是一个值） |'''

H2_OLD = u'''| **已发布成品** | `release\\PotatoST-0.11.jar` = `303c5d468b96826ef6836b0a4e54ccb8a539557c`（4,298,939 B，**ZF114 打的**：432 键、含 ZF104~ZF114）⚠ `.sha1` 的格式本轮已对账成**纯哈希一行**（§4.92） | ⚠ **不含 ZF116/ZF117** |
| 未发布的构建 | `build\\libs\\potato_s_t-0.11.jar` = `ded5b30292b53547f04922f290e4ce0b6aca76af`（4,308,290 B，ZF119 打的：449 键 + 35 条进度 + 振金锭动画贴图）⚠ 里面**含 ZF116 尚未提交的三张盔甲贴图** —— 打包轮必须重打 | 主项目线 |'''
H2_NEW = u'''| **已发布成品** | `release\\PotatoST-0.11.jar` = `303c5d468b96826ef6836b0a4e54ccb8a539557c`（4,298,939 B，**ZF114 打的**：432 键、含 ZF104~ZF114）⚠ `.sha1` 的格式已对账成**纯哈希一行**（§4.92） | ⚠ **不含 ZF116~ZF121** |
| 未发布的构建 | `build\\libs\\potato_s_t-0.11.jar` —— **打包轮必须以当时重打的为准**（并行两条线都在改 Java，历史上这个哈希每轮都变；ZF119 那次是 `f0a984b0f92e0cb6e94e26783a926c845fbbba64` / 4,308,293 B） | 主项目线 |'''

H3_OLD = u'''- **最近动作**：**ZF117 进度树补线**'''
H3_NEW = u'''- **最近动作（ZF121）**：**振金的合金冶炼炉配方**（5 输入 + 2 消耗品 → 1 振金锭，14500 FE/t）。
  顺带补两处**地基**（硬质钛合金 / 热力金属 本轮才挂上 `#c:ingots`，否则输入槽根本放不进去）、
  补掉两处「表 ↔ 盘」漂移（振金登记进 `GenCommonTags.py`）、撤掉菜单里那层"消耗槽恒放不进"的门，
  并拆掉一个**探针抓到的真雷**（星璨钢那条借了 `MAX_ENERGY_PER_TICK`，被本轮抬高的常量静默改数，§4.100）。
  **槽位数一个都没动**（用户第一版给了 4 样消耗品、自己改口收窄成 2 样）。
  更早（ZF117）：**ZF117 进度树补线**'''
H3_TAIL_OLD = u'''  更早：ZF100~ZF115（燃烧反应室'''
H3_TAIL_NEW = u'''  再早：ZF100~ZF115（燃烧反应室'''

H4_OLD = u'''## 3. 全门快照：**20 绿 / 20 红**（ZF119 轮末；红的**不是**都有罪，逐条看原因）

跑法：`python build\\zftools\\_zf119_gatesnap.py`（只读，不改盘；输出也在 `build\\zftools\\_zf119_gatesnap.txt`，**40 道门**）。'''
H4_NEW = u'''## 3. 全门快照：**见 `_zf121_gatesnap.txt`**（ZF121 轮末；红的**不是**都有罪，逐条看原因）

跑法：`python build\\zftools\\_zf121_gatesnap.py`（只读，不改盘；输出也在 `build\\zftools\\_zf121_gatesnap.txt`，**41 道门** —— 加了 ZF121 自己那道）。
⚠ **本轮开工时并行那条线（ZF120 振金套）正在同一棵树上改 Java**，他们的中间态可能让快照出现
"与 ZF121 无关的红"（比如 `ModVibraniumSet` 编译不过时我这边 `runServer` 会报
`Failed to load class com.potatost.mod.PotatoST` —— 那是**并发编译竞态**）。'''

H5_OLD = u'''- ⚠ 仍然**没有配方**的是 **星轨坠 / 粗振金**（ZF114 明说「先不给」）⇒ 它们那条成就
  我做成了**隐藏彩蛋位**（挂根、hidden），生存里点不亮是**如实反映**，不是漏做。'''
H5_NEW = u'''- ⚠ 配方状态**变了**（ZF118 + ZF121）：**星轨坠**已有合成配方（ZF118）；
  **粗振金**仍然没有合成配方，但它**现在是合金冶炼炉那条振金配方的消耗品**（ZF121，1 个）——
  所以 ZF114 那句话只剩"没有工作台配方"这一半成立。那条隐藏彩蛋位是否挪进主线，用户一句话就改。
- ⚠ **ZF120 这个轮号是你们（振金套）占的**：盘上已经有 `_zf120_lang.py` / `_zf120_src.py` /
  `ModVibraniumSet.java` / 四语言 +5 键。我这一轮**主动避开**，用的是 **ZF121**，并且没碰
  `_zf120_*` 任何文件。'''

H6_OLD = u'''    下不为例：**要挂探针的轮次，`PotatoST.java` 必须进改前件清单**。'''
H6_NEW = u'''    下不为例：**要挂探针的轮次，`PotatoST.java` 必须进改前件清单**。
14. **ZF121 的账**：① 用户第一版给了 4 样消耗品、我按字面把消耗槽扩到 4 个，他随即改口收窄成 2 样
    ⇒ 扩槽改动**已整个撤掉**（槽位数与 ZF49 一致），两代改动留档在 `_zf121_java_v1.py` +
    `_zf121_java.py --proof`（四代同堂反推 6/6），**要再扩是一处常量 + 界面两行**；
    ② **振金锭现在有来源了**（合金炉），ZF119 那条常驻断言"任何配方产物都不许是它"已改成
    "不许有**数据包/工作台**配方"（口径收窄，判据没放宽）；
    ③ **硬质钛合金 / 热力金属 这轮才挂上 `#c:ingots`** ⇒ 副作用是**别的地方也能拿它们当锭**了
    （有意，与轻质钛合金同一条口径）；要不要更细的标签（比如 `c:ingots/hard_titanium_alloy`
    不许被别的配方认）用户说了算；
    ④ **时长仍是我按本机规格补的**（用户没给 ⇒ 30 秒 ⇒ 一件 8,700,000 FE），
    另外缓冲 32768 只够 2.26 秒（要么持续供电、要么以后给他调）；
    ⑤ 我**撤掉的菜单那层门**改变了 ZF49 的一条老行为（「消耗槽目前放不了东西」）——
    如果你认为那两格就该只让自动化塞，说一声，撤回来是一处（删掉那段 override 即可）。'''

EDITS = [(DOC, A4_OLD, A4_NEW, u"§4.100 新雷"),
         (HAND, H1_OLD, H1_NEW, u"§1 键数 454"),
         (HAND, H2_OLD, H2_NEW, u"§1 成品/构建行"),
         (HAND, H3_OLD, H3_NEW, u"§2 最近动作"),
         (HAND, H4_OLD, H4_NEW, u"§3 快照口径"),
         (HAND, H5_OLD, H5_NEW, u"§5.2 配方状态 + ZF120 轮号"),
         (HAND, H6_OLD, H6_NEW, u"§6 欠账第 14 条")]


def read(p):
    return io.open(p, encoding="utf-8").read()


def write(p, text):
    io.open(p, "w", encoding="utf-8", newline=u"\n").write(text)


def main(argv):
    do_write = "--write" in argv
    fails, done, cache = [], 0, {}
    for path, old, new, label in EDITS:
        if path not in cache:
            cache[path] = read(path)
        txt = cache[path]
        if new.strip()[:40] in txt and old not in txt:
            print(u"  [跳过] %-28s %s（幂等）" % (label, os.path.basename(path)))
            done += 1
            continue
        n = txt.count(old)
        if n != 1:
            fails.append(u"%s 的锚点「%s」匹配到 %d 次（应为 1）" % (os.path.basename(path), label, n))
            continue
        cache[path] = txt.replace(old, new, 1)
        print(u"  [改]   %-28s %s" % (label, os.path.basename(path)))
        done += 1

    # §5 表格行 + §9 小节
    doc = cache[DOC]
    if u"| ZF121 |" not in doc:
        A5_TAIL = u"> ZF40~ZF44 全是**电力高炉的连续改动**"
        n = doc.count(A5_ANCHOR_TAIL)
        if n != 1:
            fails.append(u"§5 表格尾锚点匹配到 %d 次（应为 1）" % n)
        else:
            doc = doc.replace(A5_ANCHOR_TAIL,
                              u"| 见 §9 |\n" + ROW + u"\n\n\n\n" + A5_TAIL, 1)
            print(u"  [改]   §5 表格追加 ZF121 行")
            done += 1
    else:
        print(u"  [跳过] §5 表格 ZF121 行（已加过，幂等）")
        done += 1
    if u"### ZF121（0.11）" not in doc:
        n = doc.count(A9_ANCHOR)
        if n != 1:
            fails.append(u"§10 锚点匹配到 %d 次（应为 1）" % n)
        else:
            doc = doc.replace(A9_ANCHOR, A9_NEW, 1)
            print(u"  [改]   §9 追加 ZF121 小节")
            done += 1
    else:
        print(u"  [跳过] §9 ZF121 小节（已加过，幂等）")
        done += 1
    cache[DOC] = doc

    if fails:
        print(u"")
        print(u"锚点对不上，**一个字节都没写**：")
        for f in fails:
            print(u"  !! " + f)
        return 1
    if do_write:
        for p, txt in cache.items():
            write(p, txt)
        print(u"\n落盘：%d 个文件" % len(cache))
    else:
        print(u"\n（只校验，没落盘；加 --write 才写）")
    print(u"通过 = %d   失败 = 0" % done)
    return 0


if __name__ == "__main__":
    sys.exit(main(sys.argv[1:]))

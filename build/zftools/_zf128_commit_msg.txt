0.11 ZF128：成就页签图标换成毒马铃薯（判据仍是微型粉碎机）+ 把"待画数"这条链改成自洽

用户原话：「成就栏换成毒马铃薯 但是成就还是粉碎机可以嘛」

先查源码定可行性（build/neoForm/.../sources.jar，即本工程编译用的那份）：
- `AdvancementTab.java:51` `this.icon = display.getIcon();`
- `AdvancementTab.java:53` 把**同一个 display** 交给根节点 widget
- `AdvancementWidget.java:162` 画的是 `this.display.getIcon()`
⇒ **页签图标与树里根节点图标是同一个字段**，拆不开；但**图标**与**判据/说明**互不相干。
用户选的是"换，判据/说明照旧"。

改动（一行）
- `new_beginning.json` 的 `display.icon.id`：`potato_s_t:micro_crusher` → `minecraft:poisonous_potato`
- **判据一个字没动**（仍是 inventory_changed + micro_crusher）、标题/描述/背景/frame/hidden/toast 全没动
- 连锁面零：语言仍 478 键、模型/贴图/配方/Java 一行没动（原版物品不进待画清单）

跟平（四份往轮件）
- `_zf70_verify.py` SPEC / `_zf107_verify.py` D3 / `_zf124_verify.py` 图标断言
- ⚠ `_zf107_adv.py`（生成器）的 `ROOT_ICON` 原来一身兼二职（判据 + 图标）且拼死 `potato_s_t:`
  前缀 ⇒ 重跑一次就会把图标写回粉碎机；本轮拆成 ROOT_ICON_CRITERION / ROOT_ICON_DISPLAY
- `_zf107_verify.py` C1/C2/C5 三条泛化判据对根节点换成专属判据（点名毒马铃薯 + 图标必须 ≠ 判据物品）

⚠ 本轮期间**素材线把银线那两张贴图画了**（13:02）⇒ 待画 15 → 13，把 ZF127 立的
"不许有自己的 png"那几条判据当场顶红（这是它们该有的行为）：
- `_zf127_verify.py` D1/D2/D2b/H1 换成正向判据（指向我们自己的贴图 + 真 PNG）
- **待画数这条链改成现问 `TextureCheck.py`**（历史手改过 8 次、每次都有人漏一处），
  并顺手把子进程编码钉成 UTF-8（`PYTHONIOENCODING`，否则管道下按 GBK 输出、正则静默失效）
- `_zf125_verify.py` E3：控制器模型加了顶面贴图 ⇒ 判据改成"每一处贴图都是自己的图且在盘上"

顺手修一处**上一轮带进来的文档伤**
- 交接文档 §6 的顺序一度是 `18头 → 19头 → 20 → 19尾 → 18尾`（按段首匹配插入的后果，ZF127 已提交）
  ⇒ 按行区间重排并自检（§4.112）

证据
- 真服务端探针 Zf128Check **21 项全绿**（含"交毒马铃薯 ⇒ 不亮 / 交微型粉碎机 ⇒ 点亮"）
- `_zf128_verify.py` **35 项**、`_zf127_verify.py` **61 项**（跟平后）
- 反证 **K223~K226 四把**（逐把咬中指定检查）
- 全门快照：只多出本轮这一道绿门，**没有让任何原本绿的门变红**（素材线那两处已跟平）

release\ 一个字没动（本轮不打包）。

# -*- coding: utf-8 -*-
u"""_zf142_docs.py —— ZF142 的档案落笔（§5 台账一行 + §9 一整段 + §4 新增三条 + 贴图清单）

规矩（§4.112/§4.125/§4.146）：**每个锚点先数出现次数（必须唯一）**；§4 的号**落笔时现算最大号 +1**
（并发环境里不许把编号写死）。

跑法：python build\\zftools\\_zf142_docs.py [--write]
"""
import io
import os
import re
import sys

try:
    sys.stdout.reconfigure(encoding="utf-8", errors="replace")
except Exception:
    pass

ROOT = r"E:\PotatoST"
DOC = os.path.join(ROOT, "docs", "开发档案.md")
TEX = os.path.join(ROOT, "docs", "贴图清单.md")
WRITE = "--write" in sys.argv

ROW_ANCHOR = u"| 见 §9 ｜ 见 §4.139~§4.145 |\n"
SEC_ANCHOR = u"## 9. 待办与已知限制\n"
R4_ANCHOR = u"## 6. 内容速查：加一样东西要动哪些文件\n"

ROW = (
    u"| ZF142 | **新建 `zf142_pre`**（**132 份**改前件：**四张星图本体** + `_zf140_verify.py` "
    u"+ `_zf140_sim.py` + 九道门 + 全部常驻校验脚本 + 2 份文档 + 旧成品与 `.sha1`；"
    u"逐份核哈希 + 回读证明 132/132，失败 0） | 0.11：**四张星图的极带加一点点横向模糊（极滤波）**"
    u"（用户原话见 §9）。① **先判因、再下药**：我原以为极区那把「扇子」是 u 方向采样混叠，"
    u"于是拿**沿 u 均匀糊 32 个纹素**做判因实验 —— 细条纹只降 10%（44.6 → 40.3）⇒ "
    u"病根不是混叠，是**星图自己的纤维结构被径向拉长**（极点附近 v 方向被放大 2.3×、u 方向被压缩 5×）；"
    u"原本要用的「物理律半径 = 0.22/tanθ」实测全图平均只改 **1.0/255**，等于没做 ⇒ 改用**线性衰减的横向箱型模糊**；"
    u"② **参数**：`y0=96` 行（33.8°）、极点半径 `r0=16` 纹素、线性衰减到 0 —— 用户要的是「一点点」，"
    u"所以选了四档里最轻的一档（另三档 128/32、160/64 的对比图留在 `_zf142_out\\cmp_sky_mystic.png`）；"
    u"③ **只动极带**：调色板**原样保留**、带外每一行的**索引逐字节不动**，只把带内按同一调色板取最近色 "
    u"⇒ 每张改 186 行（93 上 + 93 下）、带外 324 行一个字节没变；体积反而小了 "
    u"**1.29 MB → 1.07 MB**（糊过之后压得更好，成品 jar 也小了 220 KB）；"
    u"④ **疗效**：极区细条纹 20.2/17.7/13.9/23.8 → 15.4/14.2/5.5/19.0（降 24%/20%/60%/20%）、"
    u"赤道带 0 像素改动；⑤ ⚠ 本轮**连踩两次「半径单位」**（`pole_view` 返回的是 gnomonic 不是像素）："
    u"第一次量出全 nan、第二次细条纹恒 0.0x，两次都像「图没问题」⇒ 立 §4.147；"
    u"⑥ 证据：`_zf142_verify.py` **33 项 0 失败**（主心骨是 A4：**拿留底现滤一遍必须与盘上逐字节相同**）、"
    u"反证 **K1~K5 五把刀全咬住**（带外改一像素 / 换参数重滤 / 改回原样 / 动调色板 / 把参数改成 0）、"
    u"`_zf140_verify.py` **58 项 0 失败**（B9 已按本轮改写成「只动极带」）、九道门全绿；"
    u"⑦ `TextureCheck` 警告 28 → **24**（另一条线那份门同时也在改，这 4 条不是我消的）"
    u" | 见 §9 ｜ 见 §4.147~§4.149 |\n"
)

SECTION = u"""
### ZF142（0.11）四张星图的**极带横向模糊** —— **待你实测**

我上一轮汇报里说过：黑洞盖片只是把极点的"万箭穿心"**盖住**，不是治好；真正的治法是给四张星图的
极带做极滤波。用户回：「**可以尝试加一点点模糊**」⇒ 本轮就做这一件，按"一点点"来。

**要你看的**：拿书切到任意一张星图，抬头看那把扇子 —— 细条纹应当**变柔**了，但星云本身的纹路还在。
如果还想更糊一点，说一声：`python build\\zftools\\_zf142_poleblur.py --y0 128 --r0 32 --p 1.0 --write`
（对比图在 `build\\zftools\\_zf142_out\\cmp_sky_mystic.png`，五档自上而下）。

#### 一、先判因：那把扇子到底是"混叠"还是"真结构"

这是本轮最有价值的一步。我原本的判断是**采样混叠**（极点附近贴图的 u 方向被压缩 5×，
点采样自然会挑出一些列来），于是做了个**判因实验**（`_zf142_probe.py`）：同一张星图，
只在"存盘之后、贴之前"换不同的模糊律，正对极点出图比。

| 糊法 | 极区环向总 std |
|---|---|
| 原样 | 44.65 |
| 沿 u **均匀糊 8 个纹素** | 42.98 |
| 沿 u **均匀糊 32 个纹素**（糊到亲妈都不认识） | **40.33** |
| 物理律 `0.22/tanθ` | 44.30 |

**糊 32 个纹素只降 10%** ⇒ 病根不是混叠。是星图**自己的纤维结构**被径向拉长
（极点附近 v 方向放大 2.3×、u 方向压缩 5×，纤维就成了一根根放射线）。
顺带把原本打算用的"物理律"否了：它全图平均只改 **1.0/255**，做了等于没做 ——
要是我没做这个实验，直接上物理律，交出去的就是"改了但看不出变化"。

#### 二、糊多少：四档摆在眼前选

| `y0`(行) / `r0`(纹素) / `p` | sky_verdant | sky_mystic | sky_ember | sky_tarantula | 全图平均改动 |
|---|---|---|---|---|---|
| 原样 | 20.22 | 17.71 | 13.91 | 23.77 | — |
| 64 / 8 / 1.0 | 18.40 | 16.53 | 8.68 | 22.21 | 1.12/255 |
| **96 / 16 / 1.0（本轮采用）** | **15.35** | **14.20** | **5.52** | **18.97** | **2.24/255** |
| 128 / 32 / 1.0 | 10.28 | 9.94 | 3.23 | 13.33 | 3.78/255 |
| 160 / 64 / 0.7 | 3.95 | 4.07 | 1.72 | 6.83 | 6.72/255 |

（"细条纹"= 每一圈减掉**绕圈 20° 滑动平均**之后的残差 std —— 见下面 §4.148 为什么不能用总 std。）

选了**第三行**：四档里最轻的一档、细条纹降 20%~60%、而星云的纹路还在。
第四、五档明显发灰，第五档还开始出现调色板色阶（`cmp_sky_mystic.png` 最后一行肉眼可见）。

#### 三、**只动极带**：这条比"糊多少"更重要

做法上守死一条：**调色板原样保留**、**带外每一行的索引逐字节不动**，只把带内的行按**同一个**调色板
重新取最近色。于是

- 每张图只改 **186 行**（上 93 + 下 93），带外 **324 行一个字节都没动**；
- 赤道带（第 200~311 行）**差 0 个像素**；
- 体积从 **1.29 MB 降到 1.07 MB**（糊过之后调色板图压得更好，成品 jar 顺带小 220 KB）。

**主心骨判据**（`_zf142_verify.py` 的 A4）：**拿 `zf142_pre` 留底现滤一遍，必须与盘上逐字节相同**。
这一条一口气钉三件事：改的只有极带、改的正好是这条滤波（没有手改）、留底没被动过。

#### 四、验收

| 项 | 结果 |
|---|---|
| `_zf142_verify.py` | **33 项 0 失败** |
| 反证 `_zf142_falsify.py` | **K1~K5 五把刀全咬住**，还原后逐字节一致 + 校验回绿 |
| `_zf140_verify.py`（往轮常驻） | **58 项 0 失败**（B9 已改写成"只动极带"） |
| 九道门 | 全绿（⑦ TextureCheck 警告 24 / 待画 13） |

#### 五、⚠ 说清楚两件事

1. **它治的是"细条纹"，不是整个扇子**。大块的明暗对比（比如 mystic 极点左边亮、右边暗）
   是**星图自己的内容**，糊掉就等于把图改烂了 —— 那部分仍然由黑洞盖片负责遮。
2. **这是"一点点"**。要更柔和随时可以调（一条命令），但再往上走会开始看到调色板色阶，
   那就得连调色板一起重做（`zf122` 那套中位切分 + 抖动），是另一轮的活。
"""

R4 = u"""
### 4.@@N1@@ 【方法论】「该改多少」这种问题，**先做一版极端参数**看现象会不会消失（0.11 ZF142）

用户说"加一点点模糊"。我当时的判断是"极点附近的放射条纹 = u 方向采样混叠"，
并且已经算好了"物理律半径 = 0.22/tanθ"准备照着做。**要是直接做下去，这一轮就白干了**：
那条律实测**全图平均只改 1.0/255**，交出去的东西是"改了，但看不出变化"。

真正救回来的是**判因实验**：半小时里做了个 5 宫格 —— 同一张图，只在"存盘之后、贴之前"
换不同的模糊律（不糊 / 均匀糊 8 / 均匀糊 32 / 物理律 / 物理律×3），正对极点各出一张图。

| 糊法 | 极区环向总 std |
|---|---|
| 原样 | 44.65 |
| 沿 u 均匀糊 **32 个纹素** | **40.33**（只降 10%） |

**糊到亲妈都不认识，现象还在** ⇒ 病根不是混叠，是星图自己的纤维被径向拉长。
判因一改，参数才有意义（最后用的是线性衰减的横向箱型模糊，比物理律强 20 倍）。

**规矩**：调参之前，先问"**如果现象 X 消失，应该长什么样**"，然后用一个**夸张到不可能对**的参数
去验一次。极端参数是**探测器**，不是候选方案 —— 它花的半小时，比后面调十轮参数都值。

### 4.@@N2@@ 【陷阱】指标选错 = 自己骗自己：总 std 被「真实内容」主导（0.11 ZF142）

第一版疗效指标是「极区环向总 std」，四张图糊完的降幅是 **9% / 66% / 13% / 71%** ——
差得离谱，看着像"对某几张图有效、对另几张没用"。根因：那个数被**大块明暗带**（真实内容）主导，
mystic 极点左边亮右边暗，那一大块对比度根本不是"条纹"。

换成「每一圈**减掉绕圈 20° 滑动平均**之后的残差 std」（细条纹）之后，四张的降幅变成
**24% / 20% / 60% / 20%**，一致了，也能解释（ember 的极带本来最"花"）。

**规矩**：一个指标要是给出"有的灵有的不灵"的结论，**先怀疑指标**，别急着怀疑方案 ——
尤其当被量的东西里混着"真实内容"和"要治的毛病"两种成分时，**先把不治的那部分减掉**。

### 4.@@N3@@ 【陷阱】同一个函数返回两种「半径」：gnomonic 与像素（0.11 ZF142，**同一个坑踩两次**）

`_zf142_look.py` 里 `pole_view()` 返回的径向坐标是 **gnomonic 坐标（= tanθ）**，不是像素。
它在 `ring_std` 里当半径用是对的（阈值也按 gnomonic 给），但：

- 第一次：`ring_hf` 也拿它当**像素**半径 ⇒ 圈半径 0.05~0.36 px ⇒ 全落在同一个像素上 ⇒ 量出**全 nan**；
- 第二次：修了之后 `ring_hf` 又写了一次 gnomonic ⇒ 这次的症状变成**细条纹恒 0.0x**
  （信号被抽成一个点，减掉自身当然恒 0）—— **看着像"图特别干净"，差点就信了**。

两次都发生在"判据自己哑掉"的形态：**不是报错，是安静地给出一个好看的数字**。
⇒ 立规矩：**量半径/距离的函数，参数名必须带单位**（`r_px` / `r_gnomonic`），
调用点也要把换算写出来（本轮最后写成 `f * r_in`），别让"同一个 float 两种含义"在同一个文件里流动。
"""

TEX_APPEND = u"""

## ZF142（0.11）：四张星图的**极带横向模糊**（极滤波）

用户原话（接在 ZF140 汇报里"条纹只是被盖住、没治好"那段之后）：「可以尝试加一点点模糊」

### 一、改了什么

| 文件 | 改动 | 带外 |
|---|---|---|
| `sky_verdant.png` | 186 行（上 93 + 下 93） | 324 行**逐字节未动** |
| `sky_mystic.png` | 同上 | 同上 |
| `sky_ember.png` | 同上 | 同上 |
| `sky_tarantula.png` | 同上 | 同上 |

参数：`y0=96` 行（33.8°）、极点半径 `r0=16` 纹素、线性衰减到 0（`_zf142_poleblur.py`）。
**调色板原样保留**（256 色一个没换），带内按同一调色板取最近色 ⇒ 不算"重新出图"，
是**在同一张图上做局部处理**。

体积：**1.29 MB → 1.07 MB**（糊过之后调色板图压得更好）。

### 二、疗效

极区细条纹（正对极点，3°~20° 环带，减掉绕圈 20° 滑动平均的残差 std）：

| | verdant | mystic | ember | tarantula |
|---|---|---|---|---|
| 改前 | 20.22 | 17.71 | 13.91 | 23.77 |
| 改后 | **15.35** | **14.20** | **5.52** | **18.97** |
| 降 | 24% | 20% | 60% | 20% |

赤道带（第 200~311 行）：**差 0 个像素**。

### 三、验收

- `_zf142_verify.py` **33 项 0 失败**（A4 = 拿留底现滤一遍与盘上逐字节相同）。
- 反证 **K1~K5 五把刀全咬住**（带外改一像素 / 换参数重滤 / 改回原样 / 动调色板 / 参数改成 0）。
- 四张星图仍能被模拟器读入并渲染，整图 std 43.6~62.2（没被糊死）。
- 黑洞盖片贴图本轮**一个字节没动**（sha1 `61c1032e…`）。
"""


def patch(text, anchor, insert, tag, before=True):
    n = text.count(anchor)
    assert n == 1, u"锚点[%s]命中 %d 次" % (tag, n)
    return text.replace(anchor, (insert + anchor) if before else (anchor + insert), 1)


def main():
    doc = io.open(DOC, "r", encoding="utf-8", newline="").read()
    tex = io.open(TEX, "r", encoding="utf-8", newline="").read()
    n0, t0 = len(doc), len(tex)
    print(u"改前：开发档案 %d 字符 / 贴图清单 %d 字符" % (n0, t0))

    assert u"| ZF142 |" not in doc, u"§5 里已经有 ZF142 那一行了"
    nums = [int(m.group(1)) for m in re.finditer(r"^### 4\.(\d+) ", doc, re.M)]
    base = (max(nums) + 1) if nums else 147
    for i in (base, base + 1, base + 2):
        assert (u"### 4.%d " % i) not in doc, u"§4.%d 已经被占了" % i
    r4 = (R4.replace(u"@@N1@@", str(base))
            .replace(u"@@N2@@", str(base + 1))
            .replace(u"@@N3@@", str(base + 2)))
    print(u"现算最大号 = %d ⇒ 本轮占用 §4.%d~§4.%d" % (max(nums), base, base + 2))

    doc = patch(doc, ROW_ANCHOR, ROW, u"§5 台账行", before=False)
    doc = patch(doc, SEC_ANCHOR, SECTION, u"§9 小节", before=False)
    doc = patch(doc, R4_ANCHOR, r4, u"§4 新增三条", before=True)
    tex = tex.rstrip(u"\n") + u"\n" + TEX_APPEND

    print(u"改后：开发档案 %d 字符（+%d）/ 贴图清单 %d 字符（+%d）"
          % (len(doc), len(doc) - n0, len(tex), len(tex) - t0))
    if WRITE:
        io.open(DOC, "w", encoding="utf-8", newline="").write(doc)
        io.open(TEX, "w", encoding="utf-8", newline="").write(tex)
        print(u">>> 两份都写了")
    else:
        print(u"（只试算；加 --write 才落笔）")
    return 0


if __name__ == "__main__":
    sys.exit(main())

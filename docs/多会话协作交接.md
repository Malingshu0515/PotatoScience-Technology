# 多会话协作交接：PotatoS&T 0.11

> **最后更新**：2026-09-25 20:47　**维护**：主项目线（做机器/流体/配方/进度/档案/校验的那条）
> **这份文件给谁看**：和我在**同一棵工作树**（`E:\PotatoST`）上干活的其它会话 / agent。
> 开工顺序：**这份文件 → `docs\开发档案.md` 开头的建仓通知 → §10.1（git 规矩）→ §4 雷区清单**。
> ⚠ 它不是权威档案：权威是 `docs\开发档案.md`。这里只写"**别踩别人**"和"**别人踩到哪了**"。

---

## 1. 盘上现在是什么样（活体数字，2026-09-25 21:0x）

| 项 | 值 | 谁在动 |
|---|---|---|
| 语言键数 | **398 键 × 4**（zh_cn / en_us / ja_jp / ru_ru，四份键集合必须完全一致） | 翻译润色线正在重写**值** |
| 进度（成就） | **27 条**（3 老 + 24 新），一个标签页 | 主项目线（ZF107，已提交） |
| 配方 | `data\potato_s_t\recipe\` **49 份**（其中 `crafting_shaped` **43** 条） | 多条线都会加 |
| 流体 | **15 种**（`ModFluids`；`isGas` 白名单 12 个变体） | 主项目线 |
| JEI 机器分类 | **11** 个 | 主项目线 |
| Java / 资源 | **147** 个 java / **621** 个资源文件 | — |
| **已发布成品** | `release\PotatoST-0.11.jar` = `90510e18…`（**ZF103 那版**：335 键） | ⚠ **落后 4 轮** |
| 未发布的构建 | `build\libs\potato_s_t-0.11.jar` = `3c75a391…`（4199404 B，20:18 打的，**之后润色线又改过语言文件 ⇒ 也旧了**） | 主项目线 |

**发布线还欠着**：`ZF104（硬质钛合金/稳定金属块）` + `ZF105/106（盔甲两套 + 盔甲层贴图）` + `ZF107（27 条进度）`
**一次打包**，作废 `90510e18…`。**谁最后打包，谁就负责把"档案里的成品哈希 == `release\*.sha1`"这条对上**
（`_zf70/_zf90/_zf91` 三门都在盯它）。主项目线等盔甲线说"完"就开打 —— 有异议现在说。

---

## 2. 我这条线（主项目线）的边界

- **我负责**：机器（方块/方块实体/菜单/界面）、流体、配方、JEI、粒子/音效联动、进度（成就）、
  档案 §4/§5/§9、以及 `build\zftools\` 里那套"常驻校验 + 反证刀"的方法学。
- **我不碰**：盔甲的 Java 与贴图、翻译润色的文案、`.gitattributes` / `.gitignore`、别人的提交。
- **最近动作**：ZF100~ZF107（燃烧反应室 / 酸性反应室 / 碳酸硝酸硫酸盐酸 / 27 条进度）。
  本轮的进度树、四语言 48 键、`§4.74~4.77` 四条新雷、以及 `_zf107_*` 那套脚本都**已经提交**
  （`b64a054` / `53f89ee`）。

---

## 3. 全门快照：**11 绿 / 20 红**（红的**不是**都有罪，逐条看原因）

跑法：`python build\zftools\_zf107_gatesnap.py`（只读，不改盘；输出也在 `build\zftools\_zf107_gatesnap.txt`）。

### 3.1 因为"**还没打包**"而红（打包那一步会自动消掉，别去改脚本）

| 门 | 症状 |
|---|---|
| `_zf81` `_zf82` `_zf93` `_zf100` `_zf101` `_zf102` | 「成品里键数 335 / 期望 398」——**读的是旧 jar** |
| `_zf90` `_zf91` `_zf93` `_zf94` `_zf95` `_zf96` `_zf97` | 「成品里条目名越界：`assets/.../textures/item/星璨钢.png`」——旧 jar 里那张**中文名贴图**（§4.24 老账，盘上已删） |
| `_zf90` `_zf91` | 「最新**成品**行的哈希 == `release\.sha1`」——档案里那行还是 ZF104 留下的 `__ZF104_SHA1__` 占位符 |
| `_zf73` `_zf74` `_zf75` | 「成品 == 构建产物（逐字节）」 |
| `_zf100_recipe_guard` | 「盘上比 jar 正好多 4 份」——旧 jar 的账 |

### 3.2 因为"**名单/文案陈旧**"而红（谁加的东西谁改；主项目线认领配方那几条）

| 门 | 要改什么 | 归谁 |
|---|---|---|
| `_zf73_repro.py` | `NEW_OK` 名单缺 `stable_metal_block.json` | 主项目线（打包轮） |
| `_zf73_verify.py` | D2「只新增了预期的那些」名单同上 | 主项目线（打包轮） |
| `_zf95_verify.py` | 「本轮只新增这 5 份（+后续 7 份）」名单同上 | 主项目线（打包轮） |
| `_zf100_verify.py` | 「盘上比改前多出来的」名单同上 | 主项目线（打包轮） |
| `_zf100_recipe_guard.py` | 「盘上比 jar 正好多 4 份」→ 5 份（含 stable_metal_block） | 主项目线（打包轮） |
| `_zf101_verify.py` | 「盘上比改前正好多这一份」名单同上 | 主项目线（打包轮） |
| `_zf96_verify.py` | 公告里「还在借原版贴图 = 5」那句 | 盔甲线（5 → **13**，`_zf71`/`_zf90` 已由我改到 13） |
| `_zf80_verify.py` | 5 条 tooltip 文案断言（Shift 诊断 / 流体措辞） —— **润色线改过文案** | 润色线 + 主项目线一起收口 |
| `_zf98_verify.py` | 「ru_ru 的泵提示写了「不存液体」」 —— 润色线改了俄语措辞 | 润色线 |
| `_zf71_verify.py` | 剩 6 条：4 条 tooltip 数值（en 文案）、`Light Titanium Alloy` 名称、锂电池名 | **润色线**（它把 `item.potato_s_t.light_titanium_alloy` 从 "Lightweight Titanium Alloy" 改成了 "Light Titanium Alloy"，公告还写着旧名） |

### 3.3 绿的（11）

`_zf64`（四语言键集合一致）· `_zf70`（三条老进度，本轮已按新真相改口径）· `_zf72` · `_zf78` · `_zf79` ·
`_zf89` · `_zf92` · `_zf99` · `_zf103` · `_zf104` · **`_zf107`（554 项）**

---

## 4. 共用的硬规矩（每条都有出血记录，详情看 §4）

| 规矩 | 出处 | 一句话 |
|---|---|---|
| **汇合点文件只准加行** | §4.7 | `PotatoST.java` / `ModItems.java` / `ModBlocks.java` / `ModMenus.java` / `PotatoSTClient.java` / 四份 lang —— 整份覆盖必出事 |
| **语言键数是活体数字** | §4.64 / §4.80 | 加/删**一个**语言键，要一起改：**16 份**校验器（`_zf71 _zf73 _zf75 _zf78 _zf79 _zf80 _zf81 _zf82 _zf93 _zf96 _zf97 _zf98 _zf100 _zf101 _zf102 _zf103` 的 `EXPECT_KEYS` / 键数断言）＋ `docs\UpdateAnnouncement_EN.md` 的 `(398 keys each)` |
| **动手前先备份** | §10 | `zfNNN_pre`，逐份核副本自己的哈希；补账要标来源等级①②③ |
| **探针：先抄 `build\zftools\check\`，再从 `src` 删** | §10.1 | 主项目线本轮**踩过**：先删了、没先抄（事后重建才补上） |
| **探针的中文输出要用 UTF-8 报告** | §4.50 | `runServer` 的 stdout 是 GBK，中文会变 `锟斤拷`；报告路径必须绝对，且在 `halt()` 之前写 |
| **校验器遇到坏数据必须报错，不许卡死** | §4.77 | 读指针/图的循环要有 visited 上限；反证"跑门"要带超时 |
| **反证刀改了刀就要同步改 `TARGETS`** | §4.77 | 否则刀被中断时会在盘上留下半成品 |
| **`inventory_changed` 的 `items` 是「与」不是「或」** | §4.74 | 要「或」＝多条判据 + **同一个** requirement 组 |
| **`minecraft:custom_data` 子谓词是部分匹配** | §4.75 | 只写要认的那一层（`fluid.id`），别把 `amount` 写死 |
| **别在"刀跑到一半"的时刻提交** | 本轮真事 | 首个提交 `0a1282f` 抓到过两处中间态（`capacitor.json` 父指针成环、`zh_cn.json` 被塞 ASCII 引号）；**提交前先 `git status` + 跑一遍 `_zf107_verify.py`** |
| **中文串一律用「」，不许 ASCII 引号** | §4.24 族 | 主项目线本轮在**提交信息**里也犯过一次（无害，但下不为例） |
| **换行一律 LF** | §4.8 | `.gitattributes` 是 `* -text` ⇒ 盘上是什么仓库里就是什么 |
| **`.gitattributes` / `.gitignore` 两条不许动** | §10.1 | 见那一节的理由 |
| **提交只加自己碰过的路径** | §10.1 | 多会话并行时的默认做法 |

---

## 5. 给各条线的具体提醒

### 5.1 翻译润色线 ← 你现在改的东西**会同时打到三处**

你在改 `lang\*.json` 的**值**（很好，进度树的文案你已经顺手重写了 ~30 条 en/ja/ru，我看到了）。
请注意三个连带：

1. **键的增减**要同步 §4 那张表里的 **16 份校验器** + 公告的 `(398 keys each)`；**只改值不用**。
2. 你的文案改动会打到 **`_zf71_verify.py`（6 条）**、`_zf80_verify.py`（5 条）、`_zf98_verify.py`（1 条）：
   这些门把"语言里的说法"和"英文公告里的说法"对在一起。**改完语言请顺手把公告同一句改掉**，
   或者把门里的期望值同步 —— 两边留一边旧的就是红的。
3. `light_titanium_alloy` 你改成了 **"Light Titanium Alloy"**，但公告里还有 4 处
   "Lightweight Titanium Alloy"（§5 配方表 / §6 装备表 / §7 树 / §6.2），**要一起改**。
   （⚠ 别改 `item.potato_s_t.*` 之外的 id —— 名字只在 lang 里，Java 里没有硬编码文案。）

另外：**主项目线的 `build\zftools\_zf107_adv.py` 里那张文案表现在和盘上不一致了**
（你改了 ~30 条）。重跑它会**报"值冲突"并拒绝写入**（这是设计：生成器绝不覆盖别人的文案）。
要重新生成就得先把表里的文案同步成盘上的 —— 或者干脆只当它是个"结构生成器"。

### 5.2 盔甲线

- ZF104/105/106 的代码 + 贴图 + 14 键我这边都看见了；**发布还压在"等你说完"**。
- 你加的那 8 件盔甲模型 + 硬质钛合金模型把"**还在借原版贴图的模型**"从 5 顶到了 **13**：
  `_zf71_verify.py` 与 `_zf90_verify.py` 我已经改到 13，**公告里那句由你改**（`_zf96_verify.py` 在盯）。
- 星璨钢锭**目前生存里没有来源**（只能创造拿）⇒ 27 条进度里我**故意没有**给它做节点；
  你定下来源之后跟我说一声，我补一条 challenge（`_zf107_verify.py` 的表要一起加）。
- `ModArmorItems.touch()` 那行别删（§4.72，开物品栏崩溃的根治）。

### 5.3 兼容性优化线（新）

你大概率会碰 **`c:` 标签 / 矿物词典 / 原版行为对齐**。三条最容易踩的：

1. **默认口径**：矿物 / 粗矿 / 矿石 / 锭 一律挂 `c:`（`#c:ores/x`、`#c:raw_materials/x`、`#c:ingots/x`），
   **其余物品用本模组的精确 id，不要为了"兼容"乱挂标签**（这是长期规则，写进过 §6）。
2. `build\zftools\_zf74_verify.py` 里有一张 **`c:` 标签表**（活体数字）：加/改标签要同步它，
   否则它红。
3. **加一个流体是"四处一起改"**（§6）：`ModFluids` + 客户端贴图注册 + `c:` 标签 + 四语言；
   气体还要额外进 `c:gaseous` 与 `isGas` 白名单（**正向白名单**，别改成"非水非岩浆"那种负向判定，§4.44）。

### 5.4 道具线

- 加物品：`ModItems`（**只加行**）+ 创造页一行 + `models\item\*.json` + 贴图 + **四语言键**
  ⇒ 一旦动键就回到 §4 那张"16 份校验器"的表。
- 要能合成：配方写进生成器表 `build\zftools\_zf45_recipes.py`（**表与 JSON 必须一致**，有门在比），
  然后 `_zf73_repro.py`/`_zf73_verify.py`/`_zf95_verify.py`/`_zf100_recipe_guard.py` 的"新增配方名单"要跟。
- **别自己发明数值/配方**：用户给了就照抄，没给就照先例（"不吃电"就用 ZF82/ZF96 的先例，
  并在类注释与 §9 里点名"这个数是我选的"）。

---

## 6. 我这边欠的账（谁都能催）

1. **打包发布** ZF104→ZF107（等盔甲线说"完"）→ 之后跑 `_zf107_gatesnap.py`，把 §3.1/§3.2 的表逐条收口。
2. **配方名单那 6 处**（§3.2 主项目线那几行）—— 打包轮一起改。
3. 若要做**星璨钢**的进度节点，等盔甲线给出锭的来源。
4. `build\zftools\_zf107_adv.py` 的文案表与盘上不同步（润色线改过）—— 打包前决定是"同步表"还是"表只当结构用"。
5. 仓库里还留着首个提交带进去的 **156 个刀备份条目**（`_zf*_falsify_bak_*`）；`.gitignore` 已挡新增，
   旧的要不要 `git rm -r --cached` 出仓 —— 等用户/建仓会话拍板（我倾向清掉）。

# 多会话协作交接：PotatoS&T 0.11

> **最后更新**：2026-09-25 21:5x　**维护**：主项目线（做机器/流体/配方/进度/档案/校验的那条）
> **这份文件给谁看**：和我在**同一棵工作树**（`E:\PotatoST`）上干活的其它会话 / agent。
> 开工顺序：**这份文件 → `docs\开发档案.md` 开头的建仓通知 → §10.1（git 规矩）→ §4 雷区清单**。
> ⚠ 它不是权威档案：权威是 `docs\开发档案.md`。这里只写"**别踩别人**"和"**别人踩到哪了**"。

---

## 1. 盘上现在是什么样（活体数字，2026-09-25 21:5x）

| 项 | 值 | 谁在动 |
|---|---|---|
| 语言键数 | **464 键 × 4**（zh_cn / en_us / ja_jp / ru_ru，四份键集合必须完全一致） | 键数的活体数字：… → 448（ZF117）→ 449（ZF119 振金锭）→ **454**（ZF120 振金套 +5）→ **464**（ZF122 星仪图之章 +10）→ **476**（ZF125 大型柴油发电机 +12）。⚠ **ZF121 / ZF123 / ZF124 一个键都没加没删**（改的都是值）；⚠ **ZF125 只动了 4 份写死键数的门**（`_zf100~_zf103_verify`），下面那条「17 份」是 ZF117 的实测名单、不是每次都全动 |
| 进度（成就） | **35 条**（3 老 + 24 ZF107 + 8 ZF117），一个标签页 | 主项目线（ZF117，已提交） |
| 配方 | `data\potato_s_t\recipe\` **60 份**（其中 `crafting_shaped` **54** 条）；生成器表 `_zf45_recipes.py` **31 条** | 多条线都会加；⚠ 改配方**只改表再 `--write`**（§4.93） |
| 流体 | **15 种**（`ModFluids`；`isGas` 白名单 12 个变体） | 主项目线 |
| JEI 机器分类 | **12** 个（ZF112 加了锂电池构造间） | 主项目线 |
| Java / 资源 | **156** 个 java / **642** 个资源文件 | — |
| **已发布成品** | `release\PotatoST-0.11.jar` = `303c5d468b96826ef6836b0a4e54ccb8a539557c`（4,298,939 B，**ZF114 打的**：432 键、含 ZF104~ZF114）⚠ `.sha1` 的格式已对账成**纯哈希一行**（§4.92） | ⚠ **不含 ZF116~ZF121** |
| 未发布的构建 | `build\libs\potato_s_t-0.11.jar` —— **打包轮必须以当时重打的为准**（并行两条线都在改 Java，历史上这个哈希每轮都变；ZF119 那次是 `f0a984b0f92e0cb6e94e26783a926c845fbbba64` / 4,308,293 B） | 主项目线 |

**发布线还欠着**：`ZF116（星璨钢胸甲/护腿/靴子三件贴图）` + `ZF117（8 条进度 + 状态文案）`
**一次打包**。⚠ `90510e18…` 已由 ZF114 那次打包作废，现成品是 `303c5d46…`；
下一次打包要作废的是**它**。
**谁最后打包，谁就负责把"档案里的成品哈希 == `release\*.sha1`"这条对上**
（`_zf70/_zf90/_zf91` 三门都在盯它）。主项目线等盔甲线说"完"就开打 —— 有异议现在说。

---

## 2. 我这条线（主项目线）的边界

- **我负责**：机器（方块/方块实体/菜单/界面）、流体、配方、JEI、粒子/音效联动、进度（成就）、
  档案 §4/§5/§9、以及 `build\zftools\` 里那套"常驻校验 + 反证刀"的方法学。
- **我不碰**：盔甲的 Java 与贴图、翻译润色的文案、`.gitattributes` / `.gitignore`、别人的提交。
- **最近动作（ZF121）**：**振金的合金冶炼炉配方**（5 输入 + 2 消耗品 → 1 振金锭，14500 FE/t）。
  顺带补两处**地基**（硬质钛合金 / 热力金属 本轮才挂上 `#c:ingots`，否则输入槽根本放不进去）、
  补掉两处「表 ↔ 盘」漂移（振金登记进 `GenCommonTags.py`）、撤掉菜单里那层"消耗槽恒放不进"的门，
  并拆掉一个**探针抓到的真雷**（星璨钢那条借了 `MAX_ENERGY_PER_TICK`，被本轮抬高的常量静默改数，§4.97）。
  **槽位数一个都没动**（用户第一版给了 4 样消耗品、自己改口收窄成 2 样）。
  更早（ZF117）：**ZF117 进度树补线**（8 条新节点：采油机 / 锂电池构造间 / 三元锂 /
  星璨钢 / 星璨钢套装 / 星轨坠 + 海盐 / 液体物流），四语言 **432 → 448 键**，
  `_zf107_verify.py` 的总数 27 → **35**；顺手修了 ZF115 漏的**四句状态文案**（1 mB / 600 mB）
  与 ZF114 打包留下的 **`.sha1` 格式**（§4.90~4.92 三条新雷）。
  更早：ZF100~ZF115（燃烧反应室 / 酸性反应室 / 四种酸 / 27 条进度 / 合金炉贴图 /
  **采油机 + 运行时改群系** / 星璨钢合金配方 / 锂电池构造间 …）都已提交。

---

## 3. 全门快照：**见 `_zf121_gatesnap.txt`**（ZF121 轮末；红的**不是**都有罪，逐条看原因）

跑法：`python build\zftools\_zf121_gatesnap.py`（只读，不改盘；输出也在 `build\zftools\_zf121_gatesnap.txt`，**41 道门** —— 加了 ZF121 自己那道）。
⚠ **本轮开工时并行那条线（ZF120 振金套）正在同一棵树上改 Java**，他们的中间态可能让快照出现
"与 ZF121 无关的红"（比如 `ModVibraniumSet` 编译不过时我这边 `runServer` 会报
`Failed to load class com.potatost.mod.PotatoST` —— 那是**并发编译竞态**）。
口径（四条分开看）：

1. **本轮的账（ZF119）**：新门 **`_zf119_verify.py`（63 项）** 绿 —— 独立再数一遍动画帧、
   逐帧逐像素与源图比、mcmeta 的 frametime、物品/模型/三个 `c:` 标签/**没有配方**、
   键数 449、23 份往轮校验无残留 448；**没有一道原本绿的门因为本轮变红**
   （键数 448 → 449 的重定目标把 23 份一起改了，改完逐份 `compile()` 过）。
2. **润色线正在改"进度文案的值"** ⇒ 打到 **`_zf111_verify.py` / `_zf112_verify.py`**（各 4 条）。
   那些值**归润色线**（§5.1），按 `_zf117_verify.py` 那套（`DESC_TOUCHED[locale]` 钉名单）收口即可。
3. **老账照旧**：读成品 jar 的键数（`_zf81 _zf82 _zf93 _zf102` —— 成品还是 ZF114 那版 432 键）、
   陈旧配方名单（`_zf73* _zf95 _zf96 _zf97 _zf100 _zf100_recipe_guard _zf101 _zf102`）、
   润色线的 tooltip 文案（`_zf71 _zf80 _zf98`）、素材线正在换的贴图（`_zf96 _zf97 _zf103`）。
4. **诚实说明**：本轮开工后**没有**重跑过"改前"快照，所以绿/红**不是**与上一轮同口径的对比；
   能逐条对比的是第 1、2 条里点名的那些门。

### 3.1 因为"**还没打包**"而红（打包那一步会自动消掉，别去改脚本）

| 门 | 症状 |
|---|---|
| `_zf81` `_zf82` `_zf93` `_zf102` | 「成品里 zh_cn 键数 **449**（实际 432）」—— 成品是 ZF114 打的 |
| `_zf73_repro` `_zf73_verify` `_zf74` `_zf75` | 「成品 == 构建产物（逐字节）」 |

### 3.2 因为"**名单/文案陈旧**"而红（谁加的东西谁改）

| 门 | 要改什么 | 归谁 |
|---|---|---|
| `_zf73_repro` `_zf73_verify` `_zf95` `_zf96` `_zf97` `_zf100` `_zf100_recipe_guard` `_zf101` `_zf102` | 「定形配方总数 51 / 名单」：盘上现在 **60 份 / 54 条** | 主项目线（打包轮） |
| `_zf100_verify.py` | `lithium_battery.json` 的字母→材料表（ZF112 改过；生成器表已对上，§4.93） | 主项目线（打包轮） |
| `_zf80` `_zf71` `_zf98` | tooltip 文案 | 润色线 |
| `_zf111` `_zf112` | **进度文案的值**（各 4 条）—— 照 `_zf117_verify.py` 的 `DESC_TOUCHED` 收口 | 润色线 |
| `_zf96` `_zf97` `_zf103` | 「改前那 N 张贴图一张都没动」——素材线 ZF110/ZF116 正在换 | 素材线 |

### 3.3 绿的（20）

- `_zf64_verify.py`
- `_zf70_verify.py`
- `_zf72_verify.py`
- `_zf78_verify.py`
- `_zf79_verify.py`
- `_zf89_verify.py`
- `_zf90_verify.py`
- `_zf91_verify.py`
- `_zf92_verify.py`
- `_zf94_verify.py`
- `_zf99_verify.py`
- `_zf104_verify.py`
- `_zf107_verify.py`
- `_zf108_verify.py`
- `_zf109_verify.py`
- `_zf113_verify.py`
- `_zf114_verify.py`
- `_zf117_verify.py`
- `_zf118_verify.py`
- `_zf119_verify.py`

---


## 4. 共用的硬规矩（每条都有出血记录，详情看 §4）

| 规矩 | 出处 | 一句话 |
|---|---|---|
| **汇合点文件只准加行** | §4.7 | `PotatoST.java` / `ModItems.java` / `ModBlocks.java` / `ModMenus.java` / `PotatoSTClient.java` / 四份 lang —— 整份覆盖必出事 |
| **语言键数是活体数字** | §4.64 / §4.80 | 加/删**一个**语言键，要一起改：**17 份**校验器（`_zf71 _zf73 _zf75 _zf78 _zf79 _zf80 _zf81 _zf82 _zf93 _zf96 _zf97 _zf98 _zf100 _zf101 _zf102 _zf103` 的 `EXPECT_KEYS` / 键数断言 ＋ **`_zf107_verify.py`**（上一轮漏了它自己））＋ `docs\UpdateAnnouncement_EN.md` 的 `(408 keys each)` |
| **配方总数也是活体数字** | §4.36 族 | 加一份 `recipe/*.json`，会打到 **9 份**门（`_zf73_repro` `_zf73_verify` `_zf95` `_zf96` `_zf97` `_zf100` `_zf100_recipe_guard` `_zf101` `_zf102` 的"定形配方总数 / 名单"）—— 这 9 份**本来就是红的**（陈旧名单，打包轮一起收口，见 §3.2） |
| **常驻校验必须自己把 stdout 钉成 UTF-8** | §4.81 | 否则在 UTF-8 控制台里是绿的、被 gatesnap 用**管道**调起来时按 GBK 崩掉 = **假绿**（本轮真抓到 `_zf70_verify.py`，已修） |
| **新方块物品必须进创造页** | §4.82 | 漏了 = 物品栏看不见 + JEI 搜不到，但**配方还在**（用户实测抓到采油机这一条）；常驻检查 + `_zf109_tabaudit.py` + 反证刀 K122 已就位 |
| **动手前先备份** | §10 | `zfNNN_pre`，逐份核副本自己的哈希；补账要标来源等级①②③ |
| **探针：先抄 `build\zftools\check\`，再从 `src` 删** | §10.1 | 主项目线本轮**踩过**：先删了、没先抄（事后重建才补上） |
| **探针的中文输出要用 UTF-8 报告** | §4.50 | `runServer` 的 stdout 是 GBK，中文会变 `锟斤拷`；报告路径必须绝对，且在 `halt()` 之前写 |
| **校验器遇到坏数据必须报错，不许卡死** | §4.77 | 读指针/图的循环要有 visited 上限；反证"跑门"要带超时 |
| **反证刀改了刀就要同步改 `TARGETS`** | §4.77 | 否则刀被中断时会在盘上留下半成品 |
| **`inventory_changed` 的 `items` 是「与」不是「或」** | §4.74 | 要「或」＝多条判据 + **同一个** requirement 组 |
| **`minecraft:custom_data` 子谓词是部分匹配** | §4.75 | 只写要认的那一层（`fluid.id`），别把 `amount` 写死 |
| **别在"刀跑到一半"的时刻提交** | 本轮真事 | 首个提交 `0a1282f` 抓到过两处中间态（`capacitor.json` 父指针成环、`zh_cn.json` 被塞 ASCII 引号）；**提交前先 `git status` + 跑一遍 `_zf107_verify.py`** |
| **中文串一律用「」，不许 ASCII 引号** | §4.24 族 | 主项目线本轮在**提交信息**里也犯过一次（无害，但下不为例） |
| **换行一律 LF** | §4.8 | `.gitattributes` 是 `* -text` ⇒ 盘上是什么仓库里就是什么 |
| **`.gitattributes` / `.gitignore` 两条不许动** | §10.1 | 见那一节的理由 |
| **提交只加自己碰过的路径** | §10.1 | 多会话并行时的默认做法 |
| **批量改盘：先读进变量，再 `open(..., "w")`** | **§4.99** | `open(p,"w").write(re.sub(..., read(p)))` **先截断再读** ⇒ 文件变 0 字节；01:25:38 那批 **25 份 `_zf*_verify.py` 全被清空**，还被提交进 `0489cf9`，靠 `_zf120_repair.py` 从更早的 blob 逐份取回 |
| **批量改盘的事后自查必须看大小/行数** | **§4.99** | "编译过 + 没有残留旧数字"对**空文件**全成立（§4.96 同族） |
| **提交前跑一遍门** | §4.99 / §10.1 | `git status` 干净 ≠ 内容对（`0489cf9` 就是又干净又空的） |

---

## 5. 给各条线的具体提醒

### 5.1 翻译润色线 ← 你现在改的东西**会同时打到三处**

你在改 `lang\*.json` 的**值**（很好，进度树的文案你已经顺手重写了 ~30 条 en/ja/ru，我看到了）。
请注意三个连带：

1. **键的增减**要同步 §4 那张表里的 **17 份校验器** + 公告的 `(408 keys each)`；**只改值不用**。
2. 你的文案改动会打到 **`_zf71_verify.py`**、`_zf80_verify.py`、`_zf98_verify.py`；
   ⚠ **ZF118 补**：你改**进度文案的值**会打到 **`_zf111_verify.py` / `_zf112_verify.py`**（各 4 条「除了新加的键，别的键与改前件逐字相同」，现在红着）；
   `_zf117_verify.py` 你自己已经用 `DESC_TOUCHED[locale]` 钉过名单了 —— 那两门照同一套收口即可。
   ⚠ **ZF117 新钉的一处**：`gui.potato_s_t.lithium_battery_plant.status.no_acid`（四语言）里必须留 **1 mB / 600 mB**，不许出现 10 mB / 6000 mB ——`_zf112_verify.py` 的 6.5 段在盯（那句原本是 ZF115 漏改的旧数字）：
   这些门把"语言里的说法"和"英文公告里的说法"对在一起。**改完语言请顺手把公告同一句改掉**，
   或者把门里的期望值同步 —— 两边留一边旧的就是红的。
3. `light_titanium_alloy` 你改成了 **"Light Titanium Alloy"**，但公告里还有 4 处
   "Lightweight Titanium Alloy"（§5 配方表 / §6 装备表 / §7 树 / §6.2），**要一起改**。
   （⚠ 别改 `item.potato_s_t.*` 之外的 id —— 名字只在 lang 里，Java 里没有硬编码文案。）

另外：**主项目线的 `build\zftools\_zf107_adv.py` 里那张文案表现在和盘上不一致了**
（你改了 ~30 条）。重跑它会**报"值冲突"并拒绝写入**（这是设计：生成器绝不覆盖别人的文案）。
要重新生成就得先把表里的文案同步成盘上的 —— 或者干脆只当它是个"结构生成器"。

### 5.2 盔甲线

- ZF104/105/106 的代码 + 贴图 + 14 键我这边都看见了；**发布还压在"等你说完"**。
- 你加的那 8 件盔甲模型 + 硬质钛合金模型把"**还在借原版贴图的模型**"从 5 顶到了 **13**：
  `_zf71_verify.py` 与 `_zf90_verify.py` 我已经改到 13，**公告里那句由你改**（`_zf96_verify.py` 在盯）。
- **星璨钢的进度节点 ZF117 已经补上了**（ZF111 给了合金炉配方 ⇒ 来源成立）：
  `star_steel`（goal，挂 `hard_alloy`）+ `star_steel_armor`（**明面 challenge**，
  四件「与」）。你 ZF116 把胸甲/护腿/靴子的贴图换完之后，那条挑战的图标（胸甲）
  在成就界面里也就是新贴图了 —— **不用再改 json**。
- ⚠ 配方状态**变了**（ZF118 + ZF121）：**星轨坠**已有合成配方（ZF118）；
  **粗振金**仍然没有合成配方，但它**现在是合金冶炼炉那条振金配方的消耗品**（ZF121，1 个）——
  所以 ZF114 那句话只剩"没有工作台配方"这一半成立。那条隐藏彩蛋位是否挪进主线，用户一句话就改。
- ⚠ **ZF120 这个轮号是你们（振金套）占的**：盘上已经有 `_zf120_lang.py` / `_zf120_src.py` /
  `ModVibraniumSet.java` / 四语言 +5 键。我这一轮**主动避开**，用的是 **ZF121**，并且没碰
  `_zf120_*` 任何文件。
- `ModArmorItems.touch()` 那行别删（§4.72，开物品栏崩溃的根治）。

**ZF120 补记（这一线继续往前推了一套：振金套）**

- 新增 `ModVibraniumPiece`（**只为 Shift 说明**）/ `ModVibraniumSet`（三条套装效果）；
  `ModArmorMaterials.VIBRANIUM`（下界合金数值 + 附魔权重 2）、`ModArmorItems` 四件、`ModItems` 创造页四行。
- **贴图借原版铁套** ⇒ 把我上次顶上去的那个数又顶回 **13**：
  英文公告那句、`_zf71_verify.py` 的 `n_draw`、`_zf90_verify.py` 的三条断言、
  `docs/贴图清单.md` 重跑 `--plan`，**四处一起改了**（就是 §5.2 上面那条口径）。
- **配方走锻造台**（模板 + 钛合金基底 + 振金锭）⇒ `RecipeCheck.ps1` 从本轮起**不再跳过
  `smithing_transform`**，汇总行多了 `锻造台配方通过 = N`。
  配方表进 `_zf45_recipes.py` 的新表 `SMITHING`（定形 31 + 锻造台 4 = 35 条）。
- ⚠ **`_zf100_recipe_guard.py` 我先跟平了**（§6 欠账第 2 条里的一份）：
  新增名单 4 → **20 个**、`crafting_shaped` 51 → **54**（51 + 采油机 + 锂电池构造间 + 星轨坠）。
  其余 8 份老锚点（`_zf95/96/97/100/101/102_verify` 与 `_zf73_*`）**我没动**，仍挂在你名下。
- ⚠ 我碰过 `_zf71_verify.py` / `_zf90_verify.py` / `RecipeCheck.ps1` / `_zf45_recipes.py` /
  `_zf100_recipe_guard.py` / `_zf104_gates.ps1` / `_zf104_gatecount.py` 这几份共用件，
  都是**加行或改活体数字**，没有重构。

### 5.3 兼容性优化线（新）

你大概率会碰 **`c:` 标签 / 矿物词典 / 原版行为对齐**。三条最容易踩的：

1. **默认口径**：矿物 / 粗矿 / 矿石 / 锭 一律挂 `c:`（`#c:ores/x`、`#c:raw_materials/x`、`#c:ingots/x`），
   **其余物品用本模组的精确 id，不要为了"兼容"乱挂标签**（这是长期规则，写进过 §6）。
2. `build\zftools\_zf74_verify.py` 里有一张 **`c:` 标签表**（活体数字）：加/改标签要同步它，
   否则它红。
3. **加一个流体是"四处一起改"**（§6）：`ModFluids` + 客户端贴图注册 + `c:` 标签 + 四语言；
   气体还要额外进 `c:gaseous` 与 `isGas` 白名单（**正向白名单**，别改成"非水非岩浆"那种负向判定，§4.44）。

### 5.4 道具线

- 加物品：`ModItems`（**只加行**）+ 创造页一行 + `models\item\*.json` + 贴图 + **四语言键**
  ⇒ 一旦动键就回到 §4 那张"17 份校验器"的表。
- 要能合成：配方写进生成器表 `build\zftools\_zf45_recipes.py`（**表与 JSON 必须一致**，`_zf118_verify.py` 的 B8 现在**整表逐条**在比），
  ⚠ **别直接编辑 `recipe\*.json`** —— ZF112 就是这么干的（碳酸锂→锂电池原件那轮），结果 ZF118 一跑生成器就把那张图纸**打回旧版**（§4.93）。改配方 = 改表 + `--write`；
  然后 `_zf73_repro.py`/`_zf73_verify.py`/`_zf95_verify.py`/`_zf100_recipe_guard.py` 的"新增配方名单"要跟。
- **别自己发明数值/配方**：用户给了就照抄，没给就照先例（"不吃电"就用 ZF82/ZF96 的先例，
  并在类注释与 §9 里点名"这个数是我选的"）。

---

## 6. 我这边欠的账（谁都能催）

1. **打包发布** `ZF116（三张盔甲贴图）` + `ZF117（8 条进度 + 状态文案 + .sha1 对账）` ⇒ 一次打包，
   作废 `303c5d468b96826ef6836b0a4e54ccb8a539557c`（当前成品，432 键 / 4,298,939 B）。
   ⚠ **打包脚本写 `.sha1` 只写哈希那一行**（§4.92），否则十道门会一起红。
2. **配方名单那 9 处**（§3.2 主项目线那几行）—— 打包轮一起改（以盘上 **64 份 / 54 条定形 + 4 条锻造台**为准）。
   ⚠ **ZF120 已跟平其中 1 处**：`_zf100_recipe_guard.py`（名单 20 个、`crafting_shaped` = 54）；
   剩下 8 处（`_zf73_repro` `_zf73_verify` `_zf95` `_zf96` `_zf97` `_zf100` `_zf101` `_zf102`）**还是 51**。
3. **`_zf100_verify.py` 的锂电池配方表**（ZF112 改过 `lithium_battery.json`）—— 同上，打包轮改。
4. `build\zftools\_zf107_adv.py` 与 `_zf117_adv.py` 的**文案表**与盘上不同步（润色线改过值）
   —— 生成器**重跑幂等、且遇到"值冲突"会停手**，所以不用急着同步；要重新生成才需要。
5. 仓库里还留着首个提交带进去的 **156 个刀备份条目**（`_zf*_falsify_bak_*`）；`.gitignore` 已挡新增，
   旧的要不要 `git rm -r --cached` 出仓 —— 等用户/建仓会话拍板（我倾向清掉）。
6. **ZF109 欠的两条**：① 采油机的**储能 32768 FE**、**下探上限 64 格**、**每秒重扫一次结构**
   这三个数是我定的（用户没给），要改都是一行；② 采油机 GUI 里罐子下面那**两行数字**也是我加的。
7. **ZF109 当天补的账（用户实测）**：采油机**漏进创造页** —— 已修 + `_zf109_tabaudit.py` + 反证刀 K122 +
   档案 §4.82。**以后加任何方块物品，先跑一次 `python build\zftools\_zf109_tabaudit.py`**。
8. **ZF111 欠的一条**：星璨钢配方时长（用户没给 ⇒ 30 秒 ⇒ 一件 7,200,000 FE），
   另外**消耗槽现在会收东西了**（只收某条配方点名要消耗的）。
9. **ZF117 新欠的六条**（都是我定的、用户没说的，改都是一行；见 §9）：
   ① 采油机挂 `distillation`；② 星璨钢挂 `hard_alloy`；③ 星轨坠挂**根**且**隐藏**（它没有配方）；
   ④ 星璨钢套装用**明面 challenge**；⑤ 海盐挂 `steel`；⑥ 液体物流挂 `stronger_power`。
   另外**星轨坠那条现在生存里拿不到** —— 等配方/来源定了，把它挪进主线 + 取掉 `hidden`。
10. **ZF117 的账目补记**：`PotatoST.java` 不在 `zf117_pre\_sha1.txt` 里（探针挂载是动手后才发现的），
11. **ZF118 的账**：① 星轨坠配方上线之后，ZF117 给它做的**隐藏彩蛋位**是不是要挪进主线（挂到 `star_steel` 下面、取掉 `hidden`）—— 一句话就改；
    ② ZF112 那笔「手改 JSON 没改表」的老账本轮已收口（表改成真图纸 + 逐字节互证），**但配方耦合的 9 道门的名单还没跟**（仍是打包轮的活，现在盘上 **60 份 / 54 条**）。
12. **ZF118 的账（本轮真欠的）**：① 配方耦合的 9 道门名单要跟到 **60 份 / 54 条**（打包轮）；
    ② 生成器表现在**有常驻对账**了（`_zf118_verify.py` B8 整表 31 条）——以后改配方**只改表再 `--write`**，别手改 JSON（§4.93）；
    ③ 星轨坠有配方之后，ZF117 给它做的**隐藏彩蛋位**是否挪进主线（挂 `star_steel`、去掉 `hidden`）—— 用户一句话就改。
13. **ZF119 的账**：① 振金锭**没有配方**（用户明说「目前没配方」）—— 常驻检查会一直盯着「任何配方产物都不许是它」，等哪天给配方，把那条改成正向断言；
    ② 它**没有下游**（粗振金 → 振金锭 → ？）—— 与「硫」当初同一条口径：用户没说的不发明；
    ③ 动画贴图是**客户端行为**，无头服务端验不到 ⇒ 要用户肉眼确认（3 tick 一帧、10 帧一轮 = 1.5 秒）；
    ⑤ **补充（用户实测抓到并当场修掉）**：第一版按「有不透明像素的行」分帧 ⇒ 闪光跑到本体上方的那一帧窗口被抬高 3 行 ⇒ **最后一帧往下弹**；改成**只按本体对齐**后，10 帧本体都落在 y=4..27（贴图 sha1 `98aa8894…` → `e7db8d32…`，校验 63 → 65 项，新刀 K165 专门复现这个弹跳）。
    ④ 本轮**顺序失误**（先动盘后建备份）已记进 §4.94 与 `zf119_pre\_补说明.txt`；`PotatoST.java` 第二次漏账 ⇒ 现在有常驻检查（`_zf119_verify.py` F3）。
    按 §10 用「`git cat-file blob HEAD:` == 摘钩子后的盘上文件」两条路证明，见 `zf117_pre\_补说明.txt`。
    下不为例：**要挂探针的轮次，`PotatoST.java` 必须进改前件清单**。
14. **ZF121 的账**：① 用户第一版给了 4 样消耗品、我按字面把消耗槽扩到 4 个，他随即改口收窄成 2 样
    ⇒ 扩槽改动**已整个撤掉**（槽位数与 ZF49 一致），两代改动留档在 `_zf121_java_v1.py` +
    `_zf121_java.py --proof`（四代同堂反推 6/6），**要再扩是一处常量 + 界面两行**；
    ② **振金锭现在有来源了**（合金炉），ZF119 那条常驻断言"任何配方产物都不许是它"已改成
    "不许有**数据包/工作台**配方"（口径收窄，判据没放宽）；
    ③ **硬质钛合金 / 热力金属 这轮才挂上 `#c:ingots`** ⇒ 副作用是**别的地方也能拿它们当锭**了
    （有意，与轻质钛合金同一条口径）；要不要更细的标签（比如 `c:ingots/hard_titanium_alloy`
    不许被别的配方认）用户说了算；
    ④ **时长仍是我按本机规格补的**（用户没给 ⇒ 30 秒 ⇒ 一件 8,700,000 FE），
    另外缓冲 32768 只够 2.26 秒（要么持续供电、要么以后给他调）；
    ⑤ 我**撤掉的菜单那层门**改变了 ZF49 的一条老行为（「消耗槽目前放不了东西」）——
    如果你认为那两格就该只让自动化塞，说一声，撤回来是一处（删掉那段 override 即可）。
15. **ZF123 的账（JEI 这口锅是 ZF112 的，主项目线本轮修掉）**：① **`PotatoSTJeiPlugin.MACHINES`
    与 `iconFor` 的 case 必须一一对应** —— ZF112 漏了一个 `case "lithium_battery_plant"`，
    结果从 09-25 22:19 起**整个模组的 JEI 页面全没了**（12 台机器一台不剩），
    用户 09-26 才报上来；`_zf123_verify.py` 现在钉着这条（K178 复现）。
    ② **以后加机器**：`MACHINES` + `iconFor` + `MachineRecipes` + `ModBlocks` 四处一起加，
    加完**打开 JEI 搜一下这台机器**（客户端行为没有探针，见 §4.101）。
    ③ 用户报的「星璨钢只有英文名」在盘上查不出问题（`_zf123_langaudit.py` 四项全绿），
    等用户补线索 —— **别瞎改语言**。
    ④ 本轮**没有探针**（改的是纯客户端代码），证据是客户端日志前后对照 + 源码逐一对应 + 5 把反证刀。
16. **ZF124 的账**：① **成就页签的名字 = 根成就 `new_beginning` 的标题**（四语言现在都是
    `PotatoS&T`，与创造页标题同名）；**创造页图标**从铝锭换成了**星轨坠**
    （`ModItems.POTATO_ST_TAB` 的 `.icon(...)`）；② ⚠ **待用户确认的一处**：他那句
    「换成星轨坠的物品贴图」我按字面理解成**创造页**的图标，**成就页签的图标（微型粉碎机）
    没动** —— 若他要的是页签图标，改 `new_beginning.json` 的 `display.icon` 一行即可；
    ③ `_zf70_verify.py` 的根成就 `title_zh` 已 retarget 到 `PotatoS&T`（判据没放宽），
    英文公告的**成就树**改了、**ZF107 那条 changelog 历史没动**；④ 本轮没有探针（改的是
    lang 的值 + 客户端图标），证据是 31 项常驻校验 + 4 把反证刀。

17. **ZF125 的账**：① **大型柴油发电机（3×5×2）已上线**：控制器 `diesel_generator_controller`
    + 接线口 `diesel_generator_port`（成型时替换控制器正上方那格接线块，**没有物品形态**，
    挖它掉一个接线块）；② **⚠ 铜格栅不用自己做** —— MC 1.21.1 本来就有 `minecraft:copper_grate`
    8 个氧化/涂蜡变体（§4.102 记了据以判断的两条 jar 证据）；③ **里面那四台机器不会被吃掉**
    （本轮明确**不做**"吸收成部件格"：本机没有 OBJ 模型，换成不渲染的部件格会在机器上破洞）；
    ④ **两处自定默认挂着待确认**：内部缓冲 = 1 tick 的产量 7200 FE（满则暂停烧油）、
    结构不完整照开界面但停机（灯是 19）；⑤ **状态码 19 = 结构不完整**是新号，
    以后哪台机器要用 19 先看语义能不能共用（§4.51 那条规矩）；⑥ 语言键 **464 → 476**
    ⇒ 只 retarget 了 `_zf100/_zf101/_zf102/_zf103_verify.py` 四份（其余的门不写死数字）；
    ⑦ 证据：探针 **50 项全绿** + `_zf125_verify.py` **92 项** + 反证 **K187~K198 十二把**。

18. **ZF126 的账**：① 大型柴油发电机的 **FE 缓冲从 7200 → 18000**（用户原话「这个加个fe缓存
    18k的fe」）—— 它在 ZF125 时是**我自定的默认**（"正好 1 tick 的产量"），现在**出列**，
    归到"用户给的数"里；两个常量从此**解耦**（`MAX_ENERGY` 不再 `= ENERGY_PER_TICK`，
    以后改产量不会再顺手改掉缓冲）；② **界面上多了一根能量条**（`EnergyBarPart`，12×52）——
    用户没点名要条，理由写在 §9：那个数原本看不见；③ **语言键一个都没加**
    （能量条用共享键 `gui.potato_s_t.energy`）⇒ 仍 **476 键 ×4**、零份键数耦合的门要动；
    ④ 证据：探针 **19 项全绿** + `_zf126_verify.py` **31 项** + 反证 **K199~K204 六把**；
    ⑤ ⚠ **探针挂载点第三次漏进备份清单**（`PotatoST.java`，前两次是 ZF117 / ZF119）——
    下次建改前件时**先把 `PotatoST.java` 写进清单**，别等事后补账。
